import 'package:flutter/material.dart';

class PublicarViajePage extends StatelessWidget {
  const PublicarViajePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Publicar Viaje'),
      ),
      body: const Center(
        child: Text(''),
      ),
    );
  }
}